{
    "name": "polymarket-book-vis",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "backend": "bun server/index.ts",
        "backend:dev": "bun --watch server/index.ts",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "typescript": "^7.0",
        "vite": "^8.3"
    },
    "dependencies": {
        "@polymarket/client": "^0.10"
    }
}
